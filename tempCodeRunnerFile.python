from datetime import datetime

tweet_timestamp = str(f"{datetime.timestamp(datetime.now().replace(microsecond=0)):.0}")
# tweet_timestamp = str(int(datetime.timestamp(datetime.now().replace(microsecond=0))))
print(tweet_timestamp)